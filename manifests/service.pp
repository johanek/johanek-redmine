# Class redmine:service
class redmine::service {

  # service { 'redmine':
  #   ensure  => $redmine::params::service_ensure,
  #   enable  => $redmine::params::service_enable,
  #   require => [Class['redmine::install'],Class['redmine::params']]
  # }

}
